const express = require("express");
const requestRouter = express.Router();

requestRouter.post("/send/interested/:userId", (req, res, next) => {
    try {
        
    } catch (error) {
        
    }
})

module.exports = {
    requestRouter
}